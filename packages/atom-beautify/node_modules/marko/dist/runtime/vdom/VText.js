var VNode = require("./VNode");
var inherit = require("raptor-util/inherit");

function VText(value) {
    this.bi_(-1 /* no children */);
    this.ax_ = value;
}

VText.prototype = {
    bw_: true,

    ar_: 3,

    aq_: function (doc) {
        return doc.createTextNode(this.ax_);
    },

    bc_: function () {
        return new VText(this.ax_);
    }
};

inherit(VText, VNode);

module.exports = VText;